{% extends 'base.html.twig' %}

{% block title %}CircuitCategory{% endblock %}

{% block main %}
    <h1>CircuitCategory</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ circuit_category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ circuit_category.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ circuit_category.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('circuit_category_index') }}">back to list</a>

    <a href="{{ path('circuit_category_edit', {'id': circuit_category.id}) }}">edit</a>

    {{ include('back/circuit_category/_delete_form.html.twig') }}
{% endblock %}

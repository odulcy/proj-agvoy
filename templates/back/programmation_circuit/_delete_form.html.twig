<form method="post" action="{{ path('programmation_circuit_delete', {'id': programmation_circuit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ programmation_circuit.id) }}">
    <button class="btn"> Supprimer</button>
</form>
